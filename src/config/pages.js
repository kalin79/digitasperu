import COLORS from './colors'

export const About = {
  args: {
    primaryColor: COLORS.blue.dark,
    primaryContrastColor: 'white',
    secondaryColor: COLORS.green.dark,
  }
}

export const Careers = {
  args: {
    primaryColor: COLORS.blue.dark,
    primaryContrastColor: 'white',
    secondaryColor: COLORS.pink.dark,
  }
}

export const CaseStudy = {
  args: {
    primaryColor: '#ffdc00',
    primaryContrastColor: 'black'
  }
}

export const Expertise = {
  args: {
    primaryColor: COLORS.blue.dark,
    primaryContrastColor: 'white',
    secondaryColor: COLORS.red.dark,
  }
}

export const Home = {
  args: {
    primaryColor: COLORS.pink.dark,
    primaryContrastColor: 'white',
    secondaryColor: COLORS.red.dark,
  }
}

export const Insight = {
  args: {
    primaryColor: COLORS.blue.contrast,
    primaryContrastColor: 'white',
    secondaryColor: COLORS.blue.light,
  }
}

export const Job = {
  args: {
    primaryColor: COLORS.purple.contrast,
    primaryContrastColor: 'white'
  }
}

export const Work = {
  args: {
    primaryColor: COLORS.purple.dark,
    primaryContrastColor: 'white',
    secondaryColor: COLORS.pink.dark,
  }
}
